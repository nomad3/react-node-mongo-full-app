# Docker-NodeJS-MongoDB-React for We Know APP
---------------
```
# install dependencies
$ cd server
$ npm install
$ cd ..
$ cd frontend
$ npm install
$ cd ..
$ docker-compose up
```
# run the app 
$ docker-compose up

# kill containers
$ docker-compose down
```

